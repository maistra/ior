apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: gateway-test1
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - www.bookinfo.com
    - bookinfo.example.com
